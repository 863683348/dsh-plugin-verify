# Security Policy

This plugin only reads files you point it at and performs read-only network
checks (HTTP status, npm registry metadata, GitHub API). It never writes
files and never executes scanned content.

Report issues at https://github.com/863683348/dsh-plugin-verify/issues with
the subject prefixed with [security].
